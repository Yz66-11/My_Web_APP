spring.application.name=demo

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/food?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=@Yy11091966!

#spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
#spring.datasource.username=${MYSQLUSER}
#spring.datasource.password=${MYSQLPASSWORD}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Show SQL args
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

server.address=0.0.0.0
server.port=8081

# Mail - QQ SMTP
spring.mail.host=smtp.qq.com
spring.mail.port=465
spring.mail.username=2640967068@qq.com
spring.mail.password=zyslqpvkztfrdijd
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.default-encoding=UTF-8

# File upload
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
# 5MB 以内的文件保留在内存中，不写 Tomcat 临时文件（避免 tmp/Tomcat/... 被清理导致 FileNotFoundException）
spring.servlet.multipart.file-size-threshold=5MB

# 上传文件存储根目录（相对工作目录，生产环境请覆盖为绝对路径）
# 部署时可设置环境变量：export APP_UPLOAD_DIR=/var/www/demo/uploads
app.name=\u7d2b\u97f5\u5fc3\u98df
app.upload.dir=./uploads
